[package]
name = "ai-agent-near-chain"
version = "0.1.0"
edition = "2021"

[dependencies]
near-sdk = "5.0.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
near-contract-standards = "5.0.0"

[lib]
crate-type = ["cdylib"]
